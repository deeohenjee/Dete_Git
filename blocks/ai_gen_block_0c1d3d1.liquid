{% doc %}
  @prompt
    gold announcement bar saying "Welcome To Our Store", give me the option to change the background colour

{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-announcement-bar-{{ ai_gen_id }} {
    {% if block.settings.use_gradient %}
      background: linear-gradient(135deg, {{ block.settings.background_color }} 0%, {{ block.settings.gradient_color }} 50%, {{ block.settings.background_color }} 100%);
    {% else %}
      background-color: {{ block.settings.background_color }};
    {% endif %}
    color: {{ block.settings.text_color }};
    text-align: center;
    padding: {{ block.settings.padding }}px 20px;
    font-size: {{ block.settings.font_size }}px;
    font-weight: {{ block.settings.font_weight }};
    position: relative;
    overflow: hidden;
    border-radius: {{ block.settings.border_radius }}px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);}

  .ai-announcement-bar-{{ ai_gen_id }}::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    animation: ai-shine-{{ ai_gen_id }} 3s infinite;
  }

  .ai-announcement-bar-content-{{ ai_gen_id }} {
    position: relative;
    z-index: 1;
    max-width: 100%;
    margin: 0 auto;
  }

  .ai-announcement-bar-link-{{ ai_gen_id }} {
    color: inherit;
    text-decoration: none;
    display: block;
    width: 100%;
  }

  .ai-announcement-bar-link-{{ ai_gen_id }}:hover {
    text-decoration: underline;
  }

  @keyframes ai-shine-{{ ai_gen_id }} {
    0% {
      left: -100%;
    }
    100% {
      left: 100%;
    }
  }

  @media screen and (max-width: 749px) {
    .ai-announcement-bar-{{ ai_gen_id }} {
      font-size: {{ block.settings.font_size | times: 0.9 }}px;
      padding: {{ block.settings.padding | times: 0.8 }}px 15px;
    }
  }
{% endstyle %}

<div class="ai-announcement-bar-{{ ai_gen_id }}" {{ block.shopify_attributes }}>
  {% if block.settings.link != blank %}
    <a href="{{ block.settings.link }}" class="ai-announcement-bar-link-{{ ai_gen_id }}">
      <div class="ai-announcement-bar-content-{{ ai_gen_id }}">
        {{ block.settings.announcement_text }}
      </div>
    </a>
  {% else %}
    <div class="ai-announcement-bar-content-{{ ai_gen_id }}">
      {{ block.settings.announcement_text }}
    </div>
  {% endif %}
</div>

{% schema %}
{
  "name": "Gold announcement bar",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "announcement_text",
      "label": "Announcement text",
      "default": "Welcome To Our Store"
    },
    {
      "type": "url",
      "id": "link",
      "label": "Link"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#FFD700"
    },
    {
      "type": "checkbox",
      "id": "use_gradient",
      "label": "Use gradient background",
      "default": true
    },
    {
      "type": "color",
      "id": "gradient_color",
      "label": "Gradient color",
      "default": "#FFA500"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#000000"
    },
    {
      "type": "header",
      "content": "Style"
    },
    {
      "type": "range",
      "id": "font_size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Font size",
      "default": 16
    },
    {
      "type": "select",
      "id": "font_weight",
      "label": "Font weight",
      "options": [
        {
          "value": "400",
          "label": "Normal"
        },
        {
          "value": "500",
          "label": "Medium"
        },
        {
          "value": "600",
          "label": "Semi bold"
        },
        {
          "value": "700",
          "label": "Bold"
        }
      ],
      "default": "600"
    },
    {
      "type": "range",
      "id": "padding",
      "min": 8,
      "max": 24,
      "step": 2,
      "unit": "px",
      "label": "Padding",
      "default": 12
    },
    {
      "type": "range",
      "id": "border_radius",
      "min": 0,
      "max": 20,
      "step": 2,
      "unit": "px",
      "label": "Border radius",
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "Gold announcement bar"
    }
  ],
  "tag": null
}
{% endschema %}